'use strict';

/**
 * The main class and entry point to set up a Type instance in the browser.
 *
 * @class Type
 * @constructor
 */
var Type = function () {
  console.log('Hello.');
};

Type.fn = Type.prototype;

module.exports = Type;
