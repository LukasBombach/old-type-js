/* type main */

// Base function.
var type = function() {
  // Add functionality here.
  return true;
};


// Version.
type.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.type = type;